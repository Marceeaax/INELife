{Application 'VAL02_NAME_FILE_CLAVE' logic file generated by CSPro}
PROC GLOBAL

PROC IMESPPBI_FF


PROC CI_CLAVE
STRING auxarch,file_clave;

auxarch = sysparm();


//FAJO=AUXARCH;
file_clave= strip(CI_CLAVE);

errmsg("%s",strip(CI_CLAVE));
errmsg("%s , %s",auxarch, file_clave);

if strip(file_clave) <> STRIP(auxarch) then
   WRITE("archivo:%S.csdb <> clave: %S",STRIP(auxarch),STRIP(file_clave));
endif;
